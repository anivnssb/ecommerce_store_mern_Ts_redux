import Sidebar from "../../components/Sidebar";

const TransactionManagement = () => {
  return (
    <div className="admin-container">
      <Sidebar />
      <main className="product-management">Transaction</main>
    </div>
  );
};

export default TransactionManagement;
